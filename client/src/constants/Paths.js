
const ROOT = `/`;
const LOGIN = `/login`;
const PROJECTS = `/projects/:id`;
const BOARDS = `/boards/:id`;
const CARDS = `/cards/:id`;
const SAMPLE = `/sample`;
const BASE_PATH = `http://localhost:38000`;

export default {
  ROOT,
  LOGIN,
  PROJECTS,
  BOARDS,
  CARDS,
  SAMPLE,
  BASE_PATH,
};
